{
  "name": "webgpu-fft",
  "version": "1.0.0",
  "description": "High-performance FFT library using WebGPU compute shaders",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": ["webgpu", "fft", "gpu", "signal-processing", "image-processing"],
  "license": "MIT",
  "devDependencies": {
    "@webgpu/types": "^0.1.40",
    "fast-check": "^3.15.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vitest": "^1.2.0"
  }
}
